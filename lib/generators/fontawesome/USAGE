Description:
    Install fontawesome dependencies

Example:
    rails generate fontawesome

    This will create:
        add gem 'font-awesome-rails'
        add @import "font-awesome"; >> application.scss
        add "@fortawesome/fontawesome-free": "^5.12.0", >> package.json
        yarn install
